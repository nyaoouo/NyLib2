"""Navigation: pinned-address panel + address history panel + pin gutter
state. The actual disasm/hex pin-gutter column is rendered inside
views/disasm.py and views/hex.py; this package owns the panels and the
state mutator API surface."""
