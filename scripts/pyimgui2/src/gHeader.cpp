#include "./gHeader.h"

// The unhandled-exception filter / minidump writer lives in
// UnhandledException.cpp. This translation unit is intentionally empty: it
// stays in the build so existing references to gHeader.cpp continue to resolve.
